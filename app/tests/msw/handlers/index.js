// Central handler registry for MSW tests
// Add context-specific handlers (auth, courses, ratings, enrollments) here as needed

export const handlers = [
  // Example placeholder; real handlers to be added with integration tests
]

