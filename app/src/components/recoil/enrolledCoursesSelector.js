// Legacy selector - now redirects to unified course data system
// This file exists for backward compatibility but is deprecated
import { enrolledCoursesSelector } from "./unifiedCourseDataSelectors";

// Re-export the unified selector for backward compatibility
export { enrolledCoursesSelector };
